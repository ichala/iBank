<% if @cat.length < 1 %>
<div class='text-center p-3'>
<p class='text-center text-md'>You have no categories yet 😔</p>
</div>
<% else  %>

<% @cat.each do |card| %>
 <a href="<%= group_path(card.id) %>" class="link-no">
      <div class="card m-1">
      <div class="card-body d-flex align-items-center ">
        <img src=<%= card.icon %> class="cat-icon m-1" alt="icon">
        <div class='d-flex  flex-column justify-items-center w-50 h-100' >
        <h5 class="text-muted medium-text "><%= card.name %></h5>
        <p class="text-muted small-text justify-self-end"><%= distance_of_time_in_words(card.created_at, DateTime.now) %></p>
      </div>
       <div class='d-flex align-items-center justify-content-center w-25 ' >
            <p class='text-italic link-no text-dark'><%= card.trans.sum(:amount) %> $</p>
      </div>

      </div>
</div></a>
<% end %>


<% end %>

<div class="d-flex align-content-center justify-content-center">
<%= link_to  "Add", new_group_path , class:'btn btn-success'%>
</div>

